\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{url}
\graphicspath{{figures/}}
\usepackage{fancyhdr}

% --- BECITHCON 2026 camera-ready furniture -------------------------------
% Conference header (first page, top-left) + IEEE copyright notice (first
% page, bottom-left). Both sit in zero-height boxes so they occupy the
% margins and do not shift the IEEE text block. No page numbers anywhere.
\fancypagestyle{becithconfirst}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
  \fancyhead[L]{\smash{\raisebox{-1.2ex}{\parbox{\textwidth}{\scriptsize
    2026 IEEE International Conference on Biomedical Engineering, Computer and Information Technology for Health (BECITHCON)\\
    04-05 September 2026, Department of EEE, International University of Business Agriculture and Technology (IUBAT), Uttara, Dhaka-1230, Bangladesh}}}}%
  \fancyfoot[L]{\smash{\raisebox{1.2ex}{\parbox{\textwidth}{\scriptsize
    979-8-3195-3812-3/26/\$31.00~\copyright2026 IEEE}}}}%
}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

\title{SENTINEL: Organ-System Decomposition for Robust and Interpretable
Early Warning of Sepsis in the ICU}
\author{%
\IEEEauthorblockN{Md Sazzad Siddique\IEEEauthorrefmark{1},
Rezuan Islam\IEEEauthorrefmark{2},
Md.\ Mashrurul Haque\IEEEauthorrefmark{2},
Konak Mozumder\IEEEauthorrefmark{3},
Md Saidul Islam Arif\IEEEauthorrefmark{2}}
\IEEEauthorblockA{%
\IEEEauthorrefmark{1}\textit{Information Technology Department, Incepta Pharmaceuticals Ltd.}, Dhaka, Bangladesh \\
\IEEEauthorrefmark{2}\textit{University of Wales Trinity Saint David}, London, United Kingdom \\
\IEEEauthorrefmark{3}\textit{American International University-Bangladesh}, Dhaka, Bangladesh \\[0.4ex]
sazzadsiddique@inceptapharma.com, 2429105@student.uwtsd.ac.uk, 2435257@student.uwtsd.ac.uk, \\
20-43749-2@student.aiub.edu, 2428155@student.uwtsd.ac.uk}
}

\maketitle
\thispagestyle{becithconfirst}
\pagestyle{empty}

\begin{abstract}
Early warning of ICU sepsis improves outcomes, yet the strongest deep models are
opaque, brittle when inputs go missing, and require centralizing sensitive
records---a deployed proprietary model failed externally by latching onto clinician
ordering rather than physiology \cite{wong2021epic}. We decompose the patient into
six organ-system agents aligned with the Sequential Organ Failure Assessment (SOFA)
score, each seeing only its own subsystem, and combine their organ-specific onset
risks; the action is alerting, never treatment dosing. On a MIMIC-IV cohort of
80{,}248 ICU stays, the organ-decomposed model flags deterioration a median
13.7\,h before onset---detecting 98\% of septic episodes at $\approx$1.5 alarm
events per patient-day---and matches or beats a monolithic deep model on AUPRC
(0.257 vs.\ 0.205 on internal test, disjoint 95\% CIs), roughly doubling XGBoost.
Crucially, it is far more robust to a missing organ signal: blinding any one organ
costs it 0.002--0.005 AUPRC versus 0.03--0.04 for the monolith, and two
controls---a joint-feature ensemble and a missingness-augmented monolith---localize
this robustness to \emph{organ decomposition} itself, not ensembling or
missingness-aware training. The decomposition also yields per-organ risk
attribution---which system is deteriorating---and trains by federated averaging
across non-IID care units at a quantified privacy cost. With the MIMIC-trained
models \emph{frozen}, we externally validate on a second database (eICU; 122{,}798
stays, 208 hospitals): the decomposed model keeps the best external AUPRC (0.123)
but---we state this prominently---the \emph{weakest} external AUROC of the four
supervised models (0.658 vs.\ 0.677--0.702), so its external edge is
AUPRC-specific and not a general discrimination win. The federated privacy cost
replicates across genuine institutions ($-0.089$ vs.\ $-0.091$ AUPRC), and the
missing-signal advantage attenuates but stays in the decomposed model's
favor. We also report a principled null: cooperative multi-agent RL adds no benefit
(centralized MAPPO $\approx$ independent IPPO), as expected when alerting does not
perturb the patient. All numbers carry bootstrap CIs, and the pipeline is fully
reproducible.
\end{abstract}

\begin{IEEEkeywords}
sepsis, early warning, MIMIC-IV, interpretability, robustness, multi-agent
reinforcement learning, federated learning
\end{IEEEkeywords}

\section{Introduction}
Sepsis is a leading cause of in-hospital mortality, and early recognition improves
outcomes. Machine-learning early-warning systems are increasingly deployed, but
three concerns limit their clinical trust. First, the strongest models are
\emph{black boxes}: a single risk score offers no account of \emph{which} organ
system is deteriorating. Second, they are \emph{brittle}: ICU data are intermittently
missing, and a model trained on the full feature vector can fail silently when a
signal goes dark. Third, training typically requires \emph{centralizing} patient
records across institutions, which is often legally infeasible. A widely deployed
proprietary sepsis model was recently shown to underperform on external validation,
in part by latching onto clinician \emph{ordering behavior} rather than physiology
\cite{wong2021epic}---a cautionary tale for the whole approach.

We study a structural alternative: decompose the patient into six organ-system
agents aligned with the SOFA score \cite{vincent1996sofa}
(respiratory, coagulation, hepatic, cardiovascular, neurologic, renal), each
observing only its own subsystem, and combine their organ-specific onset risks. The
\emph{escalation action} is alerting (maintain / watch / escalate), never treatment
dosing---a deliberate choice that keeps evaluation valid and sidesteps the
off-policy-evaluation pitfalls of offline reinforcement learning (RL) for treatment
\cite{gottesman2019guidelines,komorowski2018aiclinician}. The result is an
early-warning system that, on MIMIC-IV, flags deterioration a median 13.7\,h before
onset at a low alarm burden, names the organ driving the risk, and---unlike a
monolithic deep model---barely degrades when one organ signal goes dark.

Our contributions, each tied to a controlled result:
\begin{itemize}
  \item \textbf{Clinically useful early warning}: a median 13.7\,h lead time at
        $\approx$1.5 alarm events per patient-day, matching or beating a monolithic
        deep model on AUPRC (with bootstrap confidence intervals) on internal and
        external splits.
  \item \textbf{Decomposition---not ensembling---confers missing-signal robustness},
        established by two controls: a joint-feature ensemble and a monolith trained
        with organ-dropout augmentation, both markedly less robust than decomposition.
  \item \textbf{Interpretability} via per-organ risk attribution, supported by a
        replay dashboard.
  \item \textbf{Federated feasibility at a quantified privacy cost} across non-IID
        care units.
  \item \textbf{External validation on a second database (eICU) and real
        institutions}: discrimination transfers and the federated privacy cost
        replicates across genuine hospitals, with an honest account of where the
        missing-signal robustness advantage attenuates under domain shift.
  \item \textbf{A principled null for cooperative coordination}: MAPPO $\approx$ IPPO,
        explained by the exogenous-dynamics structure of alerting.
\end{itemize}
We report every result that weakened a claim rather than hiding it.

\section{Related Work}
Rule-based early-warning scores such as NEWS \cite{rcp2017news2} are interpretable
but weak discriminators. Supervised sepsis predictors on MIMIC
\cite{henry2015trewscore,johnson2023mimiciv} are stronger but monolithic and
opaque, and external validation can expose shortcut learning \cite{wong2021epic}.
Multi-agent RL has been explored for healthcare, but cooperative methods---MAPPO
\cite{yu2022mappo}, QMIX \cite{rashid2018qmix}, MADDPG \cite{lowe2017maddpg}---are
motivated by agents perturbing a shared environment; IPPO \cite{dewitt2020ippo} is
the natural control isolating the value of a centralized critic. Federated averaging
\cite{mcmahan2017fedavg} enables training without pooling data, but non-IID clients
degrade utility \cite{zhao2018noniid}. Fairness and shortcut concerns in clinical ML
are well documented \cite{obermeyer2019bias}; we audit the latter (shortcut
reliance) directly in our ablation. We bring these threads together with
an emphasis on \emph{controlled} attribution of where decomposition helps.

\section{Methods}
\subsection{Cohort and Sepsis-3 labels}
From MIMIC-IV v3.1 \cite{johnson2023mimiciv} we build a cohort of first ICU stays
(adults 18--89, length of stay $\geq 6$\,h; 80{,}248 stays). We reimplement the
Sepsis-3 criteria \cite{singer2016sepsis3,seymour2016assessment}, cross-checked
against the MIMIC code repository \cite{johnson2018mimiccode}: suspicion of
infection (antibiotic + culture within the standard window) co-occurring with an
acute rise in hourly SOFA. We use a \emph{dynamic baseline}---the acute rise is
measured against the patient's pre-suspicion admission SOFA---giving a prevalence of
37.4\% and, crucially, a clean \emph{three-way split}: positives are stays with
onset at least $H{=}6$\,h into the stay (8{,}763); stays that are septic earlier or
present-on-admission are \emph{excluded} from both classes; controls never meet
Sepsis-3. These are distinct denominators: 37.4\% of stays are ever-septic, the
8{,}763 actionable-onset positives are $\approx$11\% of stays, and $\approx$5\% of
patient-\emph{hours} are positive (the AUPRC no-skill line). Derivations are validated
by unit tests against hand-checked stays.

\subsection{Features and the exogenous-dynamics formulation}
Per organ we form hourly features (values, missingness masks, and measurement
indicators), with physiologic forward-fill and train-split-only normalization;
a room-air FiO$_2$ default and an SpO$_2$/FiO$_2$ surrogate \cite{rice2007sfratio}
address sparse blood gases. The decision process is a decentralized partially
observable problem: each organ agent sees its own subsystem plus minimal shared
context. Critically, \emph{alerting does not change the patient}, so the observation
trajectory is exogenous: the reward of any action sequence is exactly computable from
the record. This removes the off-policy-evaluation problem entirely and frames the
learning as policy-gradient optimization of a non-differentiable clinical utility,
not decision-making under environment uncertainty.

\subsection{Models}
\textbf{SENTINEL-Risk (organ-decomposed).} Six per-organ GRU \emph{risk heads} are
trained by supervision; the team onset risk is their max-combine---the headline
discriminator compared on AUPRC, thresholded for alerting, and the source of
structural robustness (blinding one organ disables one of six heads).
\textbf{SENTINEL-Alarm (alarm policy).} A \emph{separate} policy over the organ risks
and the alert history is trained with PPO \cite{schulman2017ppo} and GAE
\cite{schulman2016gae} on a ramped early-warning utility (lead-time bonus, miss
penalty, false-alarm and per-alarm-event costs); being utility- not label-trained, it
is a weak discriminator by design and reported only for completeness.
\textbf{Cooperative variants.} SENTINEL-Alarm uses either a centralized critic over
the joint state (MAPPO) or per-organ critics (IPPO)---the only difference, isolating
coordination. \textbf{Federated.} We partition training data into the eight admitting
care units with sufficient volume (genuinely non-IID; e.g.\ MICU ${\sim}46\%$ vs.\ neuro
${\sim}14\%$ ever-septic) and train a shared model by FedAvg \cite{mcmahan2017fedavg}
without pooling data, holding out one unit (CVICU) as an external site.

\subsection{Architecture and hyperparameters}
\label{subsec:hyper}
Every sequence model here is a \emph{single-layer, causal} GRU with a linear
per-hour logit head, so the score at hour $t$ depends only on hours $\leq t$---no
look-ahead by construction. SENTINEL-Risk is six independent GRUs of 48 hidden
units, one per organ, each reading that organ's features plus the shared context,
with \emph{no} parameters shared across organs; the team logit is their
element-wise maximum. GRU-single, GRU-masked and each JointEnsemble-6 member use
96 hidden units over the full feature vector, so the monolithic controls are not
disadvantaged on capacity. Supervision is per-hour binary cross-entropy on the
logits, positively weighted by the empirical negative/positive ratio and masked
over padded hours, with the target being onset within a $1$--$6$\,h lead window;
optimization is Adam with early stopping on validation AUPRC. SENTINEL-Alarm is a
separate GRU actor--critic over the six organ risks plus alert history, trained by
PPO. Table~\ref{tab:hyper} gives the full settings.

\begin{table}[t]
\caption{Architecture and training hyperparameters (fixed on MIMIC, reused
unchanged on eICU). All GRUs are single-layer and causal; FP32, one 4\,GB GPU.}
\label{tab:hyper}
\centering\footnotesize\setlength{\tabcolsep}{4pt}
\begin{tabular}{ll}
\toprule
Component / setting & Value \\
\midrule
SENTINEL-Risk heads & $6\times$ GRU(48) + linear, unshared \\
GRU-single, GRU-masked & GRU(96) + linear, full vector \\
JointEnsemble-6 member & GRU(96) + linear, joint vector \\
SENTINEL-Alarm & GRU(64) actor, critic width 128 \\
\midrule
Supervised loss & pos-weighted BCE, padding masked \\
Label window & onset within 1--6\,h \\
Optimizer / LR & Adam / $10^{-3}$ (PPO $3{\times}10^{-4}$) \\
Batch (episodes) & 256 heads, 64 baselines, 128 PPO \\
Max epochs / updates & 40 heads, 25 baselines, 80 PPO \\
Early stopping & val.\ AUPRC, patience 5 \\
PPO & clip 0.2, $\gamma$ 0.99, GAE $\lambda$ 0.95, \\
 & entropy 0.01, value 0.5, clip-norm 1.0 \\
Organ-block dropout & $p{=}0.5$ (GRU-masked only) \\
Seeds & 3--5 \\
\bottomrule
\end{tabular}
\end{table}

\section{Experimental Setup}
Splits are temporal by anchor-year group (train 2008--2016 / val 2017--2019 / test
2020--2022), with the cardiac-vascular ICU held out entirely as the external site.
Baselines: NEWS2 \cite{rcp2017news2}, XGBoost, and a monolithic GRU. The primary
metric is AUPRC (class imbalance $\approx$5\% positive hours) with 95\% bootstrap
confidence intervals resampled over \emph{stays}; we also report AUROC, the alert
lead time, and the alarm burden as distinct \emph{alarm events} per patient-day
(rising edge with a refractory window), with the operating threshold chosen on
validation at a fixed budget. Learned models use 3--5 seeds (robustness and the organ-decomposed
external rows use 3, other runs up to 5; full-input baselines thus differ by
$\leq$0.01 AUPRC across analyses, so robustness is reported as within-run deltas).
Compute is a single
4\,GB GPU; all training is FP32.

\textbf{External validation.} As a second, independent test we score the
\emph{frozen} MIMIC-trained models on eICU-CRDB v2.0 \cite{pollard2018eicu}, a
multi-center U.S.\ database, harmonized into the identical organ-feature schema and
normalized with the MIMIC training statistics---no gradient ever updates on eICU
(asserted by a unit test). We replicate the Sepsis-3 label and episode logic; because
eICU culture coverage is sparse, suspicion of infection is antibiotic-primary, which
we treat as a documented label-fidelity deviation. The external cohort is 122{,}798
modeling stays across 208 hospitals (no-skill AUPRC 0.036), and for federation we use
genuine hospital identifiers as sites.

\section{Results}
\subsection{Discrimination}
Table~\ref{tab:disc} and Fig.~\ref{fig:disc} show the organ-decomposed model
(SENTINEL-Risk) matching or beating the monolithic GRU---AUPRC 0.257 vs.\ 0.205 on
internal test, where the 95\% CIs are disjoint (0.233--0.283 vs.\ 0.186--0.225), and
roughly doubling XGBoost internally (0.257 vs.\ 0.115). On the held-out CVICU unit the
monolith is marginally ahead (0.283 vs.\ 0.265), with the decomposed model regaining
the lead on eICU AUPRC (Sec.~\ref{subsec:external}). The utility-trained SENTINEL-Alarm
policy (MAPPO/IPPO) is a weak discriminator ($\sim$0.06), as expected when optimizing
alarm utility rather than the label.

\begin{table}[t]
\caption{Discrimination (AUPRC; 95\% CI). No-skill: 0.049 / 0.057. The SENTINEL-Alarm
row is the utility-trained alerting policy (not a discriminator), shown for
completeness.}
\label{tab:disc}
\centering\small
\begin{tabular}{lcc}
\toprule
Model & internal test & external (CVICU) \\
\midrule
NEWS2 & 0.066 & 0.051 \\
XGBoost & 0.115 & 0.242 \\
GRU-single & 0.205 & \textbf{0.283} \\
JointEnsemble-6 & 0.202 & 0.283 \\
\textbf{SENTINEL-Risk} & \textbf{0.257} & 0.265 \\
SENTINEL-Alarm & 0.063 & 0.051 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering\includegraphics[width=\columnwidth]{fig_discrimination.pdf}
\caption{AUPRC by model with 95\% CIs and the no-skill line. Organ decomposition
(red) matches/beats the monolithic deep model.}
\label{fig:disc}
\end{figure}

\subsection{Lead time and alarm burden}
At the validation-chosen operating point (alarm budget $\leq$8 events/patient-day),
the models reach comparable detection at a similar burden (Table~\ref{tab:clin}). The
decomposed model detects 98\% of positive episodes at a median lead time of 13.7\,h
and 1.5 distinct alarm events per patient-day---matching the monolith on burden and
sensitivity, with a slightly \emph{shorter} lead time and marginally higher control
false-alarm rate. We report this plainly: the decomposition's contribution is
robustness and interpretability, not earlier or quieter alerting, on which it is on
par with the monolithic baseline (the pattern holds on the held-out CVICU unit,
lead 6.0\,h vs.\ 8.0\,h).

\begin{table}[t]
\caption{Clinical metrics at the validation-set operating point (internal test;
alarm budget $\leq$8/patient-day; 3 seeds). Lead = median hours before onset over
detected positives; FA = fraction of control stays raising $\geq$1 alarm.}
\label{tab:clin}
\centering\small
\begin{tabular}{lcccc}
\toprule
Model & Sens. & Lead (h) & Alarms/day & FA \\
\midrule
NEWS2 & 0.96 & 18.0 & 2.6 & 0.93 \\
XGBoost & 0.97 & 16.0 & 1.8 & 0.78 \\
GRU-single & 0.97 & 15.3 & 1.5 & 0.79 \\
\textbf{SENTINEL-Risk} & 0.98 & 13.7 & 1.5 & 0.81 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interpretability: per-organ attribution}
Because the team score is the \emph{maximum} over the per-organ risk heads, every alert
is by construction attributable to the organ currently driving it: the model reports not
only \emph{that} risk is rising but \emph{which} system is responsible. Fig.~\ref{fig:attr}
illustrates the mechanism---as one organ's risk leads, the team trace inherits its
identity and the alert names that system, hours ahead of onset. This per-organ rationale
is a structural property of the decomposition that a monolithic single-score model cannot
offer; the replay dashboard exposes the same decomposition interactively on real stays.

\begin{figure}[t]
\centering\includegraphics[width=\columnwidth]{fig_attribution.pdf}
\caption{Per-organ risk attribution---an \emph{illustrative schematic} of the mechanism
(not a single real patient; the dashboard renders the same decomposition on real stays).
Each colored trace is one organ agent's onset risk and the team score is their
max-combine (black). Because the team inherits the leading organ's risk, every alert
names a driving system---here the cardiovascular agent carries the team across the
threshold $\approx$14\,h before Sepsis-3 onset---an \emph{attributable} early warning a
single-score model cannot give.}
\label{fig:attr}
\end{figure}

\subsection{Missing-signal robustness, controlled}
Fig.~\ref{fig:rob} is the central result. Blinding any one organ's inputs at test
time costs the decomposed model a mean of $-0.002$/$-0.005$ AUPRC (internal test /
held-out CVICU), versus $-0.028$/$-0.041$ for the monolithic GRU. Two controls
localize the cause. First, \textsc{JointEnsemble-6}---six GRUs on the \emph{joint}
vector with the same max-combine---degrades like the monolith ($-0.030$/$-0.056$) and
at full inputs is no stronger than it (both $\approx$0.20, well below the decomposed
0.257): neither the robustness nor the discrimination gain comes from ensembling.
Second, to rule out the simpler reading that the monolith is fragile only because it
never saw missing inputs during training, we train \textsc{GRU-masked}---the same
monolithic GRU with organ-block dropout augmentation (one random organ zeroed per
episode, exactly the test-time perturbation). This more than halves its degradation
($-0.011$/$-0.015$) but leaves the decomposition $3$--$5\times$ more robust:
missingness-aware training of a single network narrows the gap without closing it, so
the robustness is a structural property of \emph{organ decomposition}, not of
ensembling or of having trained on dropouts. The starkest case: with the
cardiovascular organ blinded on CVICU, the decomposed model is unmoved (0.289, above
its own full-input mean---no single organ is load-bearing) while the joint ensemble
(0.173) and plain monolith (0.176) collapse; augmentation rescues the monolith here
(0.268) but not on every organ. The decomposition is itself not uniformly
flat---its worst case, the respiratory organ, costs $-0.043$ on CVICU (comparable to
the monolith's mean, and matched by \textsc{GRU-masked} at $-0.034$)---but no organ is
catastrophic and its across-organ mean stays near zero.

\begin{figure}[t]
\centering\includegraphics[width=\columnwidth]{fig_robustness.pdf}
\caption{Held-out CVICU AUPRC as each organ is blinded. Organ-decomposition (red) stays
flat; the joint-feature ensemble and the monolithic GRU collapse, and even a monolith
trained with organ-dropout augmentation (\textsc{GRU-masked}, orange) degrades more than
the decomposition.}
\label{fig:rob}
\end{figure}

\subsection{What the non-physiology signal is}
Fig.~\ref{fig:abl} decomposes the gap between physiology-only and full inputs. With
tight confidence intervals on the full cohort, dropping the measurement/ordering
channels costs essentially nothing ($-0.002$, not significant), while dropping the
temporal prior costs the rest (the deep model $-0.07$, XGBoost $-0.02$). The
non-physiology signal here is a benign timing prior---base-rate hazard over stay-time,
which the leakage test and length-matched control episodes keep from encoding onset
directly---\emph{not} the clinician-ordering shortcut that has undermined
deployed models \cite{wong2021epic}.

\begin{figure}[t]
\centering\includegraphics[width=0.86\columnwidth]{fig_ablation.pdf}
\caption{Ablation: the non-physiology signal is the timing prior, not the
measurement/ordering leak.}
\label{fig:abl}
\end{figure}

\subsection{Coordination is a measured null}
SENTINEL-Alarm is statistically indistinguishable with a centralized (MAPPO) or
independent (IPPO) critic (test AUPRC 0.063 vs.\ 0.066, overlapping CIs; unmoved by
$5\times$ training). This is principled, not a failure: under exogenous alerting
dynamics no agent perturbs the environment, so a centralized critic has nothing to
coordinate---and an action-history-conditioned actor matched the observation-only
policy, justifying the simplification. The clinical consequence is concrete: an
alerting system of this kind needs no centralized critic and no inter-agent
communication at inference, so each organ head can be trained, audited, revised,
or---when its signal is unavailable---dropped independently at the bedside, and a
site deploying SENTINEL can do so without the coupling and compute that
cooperative machinery would add for no measured benefit.

\subsection{Federated privacy cost}
Across eight non-IID care units, FedAvg reaches AUPRC 0.121 (test) / 0.237 (held-out
CVICU) versus 0.212 / 0.290 centralized---a real cost of $-0.091$ / $-0.053$, yet still well
above no-skill. Privacy-preserving training is feasible but not free when sites are
strongly non-IID \cite{zhao2018noniid}.

\subsection{External validation: a second database and real institutions}
\label{subsec:external}
Table~\ref{tab:eicu} reports the frozen models on eICU. \emph{Discrimination
transfers}: the supervised deep models retain 88--91\% of their internal AUROC
(XGBoost 97\%). The organ-decomposed model keeps the best external AUPRC (0.123), but
on the prevalence-independent AUROC it is the weakest of the four supervised models
(0.658 vs.\ 0.677--0.702 for XGBoost, the monolith, and the joint ensemble)---its
external edge is AUPRC-specific, and we report plainly that AUROC, the cleaner
cross-database metric, does not favour the decomposition. AUPRC falls in
absolute terms, but this tracks the lower eICU prevalence (no-skill 0.036 vs.\ 0.049
internally), not a ranking collapse---so we read cross-database transfer off AUROC.
NEWS2 and SENTINEL-Alarm stay weak, as internally; the alarm policy in fact
transfers slightly \emph{better} to 208 real hospitals (AUROC 0.561) than to the single
simulated CVICU unit (0.435), a reminder that one held-out care unit is a noisier
external proxy than a genuine multi-center test.

\begin{table}[t]
\caption{External validation on eICU (122{,}798 stays, 208 hospitals; no-skill AUPRC
0.036). Frozen MIMIC-trained models---no eICU gradient updates. AUROC is the
prevalence-independent cross-database metric.}
\label{tab:eicu}
\centering\small\setlength{\tabcolsep}{3pt}
\begin{tabular}{lccc}
\toprule
Model & int.\ AUROC & eICU AUROC & eICU AUPRC \\
\midrule
NEWS2 & 0.588 & 0.559 & 0.044 \\
XGBoost & 0.701 & 0.677 & 0.076 \\
GRU-single & 0.756 & 0.687 & 0.092 \\
JointEnsemble-6 & 0.769 & 0.702 & 0.092 \\
\textbf{SENTINEL-Risk}$^{\dagger}$ & 0.751 & 0.658 & \textbf{0.123} \\
SENTINEL-Alarm & 0.609 & 0.561 & 0.045 \\
\bottomrule
\end{tabular}

{\footnotesize $^{\dagger}$External figures from the robustness full-input condition
(3 seeds); the 5-seed external run behind the other rows predates this model variant.}
\end{table}

\emph{The robustness gap attenuates under domain shift.} Re-running the per-organ
blinding on eICU, the organ-decomposed model remains the most robust (mean $-0.003$
AUPRC) but the controls degrade far less than internally (JointEnsemble-6 $-0.006$,
monolith $-0.007$, versus $-0.030$/$-0.028$ on MIMIC). The cause is mechanical:
externally the controls already sit near the signal floor (full-input AUPRC $\approx$
no-skill), leaving little for blinding to remove. The advantage does persist where
there is signal to lose---on the high-signal respiratory and cardiovascular organs the
decomposed model is flat ($-0.001$) while the controls drop $-0.012$/$-0.014$---but we
do not claim the full internal gap transfers.

\emph{The federated privacy cost replicates on real institutions.} Using genuine
hospital identifiers as sites---78 hospitals with sufficient sepsis-positive volume,
plus 15 held out entirely as unseen institutions---FedAvg costs $-0.089$ AUPRC versus
centralized ($0.197\!\to\!0.108$), almost identical to the $-0.091$ measured over
simulated care units above. The cost is thus a property of non-IID clinical
federation, not an artifact of the care-unit proxy. The centralized model generalizes
to the 15 unseen hospitals exactly as well as in-distribution (0.197 on both), and
federation's cost there is slightly smaller ($-0.075$). One heterogeneity has no
care-unit analog: 38 hospitals with $\geq$200 stays report zero sepsis (incomplete
medication/culture reporting) and must be excluded from the federation.

\section{Discussion}
The decomposition buys exactly what a monolithic model cannot: per-organ
attribution and a structural fallback when a signal is missing, at no net
discrimination cost---it matches or beats the monolith on the primary AUPRC metric
across splits. The cooperative-MARL machinery, by contrast, adds nothing here,
and we believe the exogenous-dynamics analysis explains why and generalizes to other
\emph{alerting} (as opposed to \emph{acting}) clinical tasks. External validation
sharpens the picture: discrimination and the federated privacy cost both transfer to
real institutions, whereas the robustness advantage---though it keeps the decomposed
model on top---shrinks once the baselines lose their signal to domain shift, an effect
only a genuine second database could expose. The honest takeaway is that
\emph{structure}, not \emph{coordination}, is the useful inductive bias.

\textbf{Where the external evidence is weakest.} The decomposition's external
advantage is AUPRC-specific and we do not want that read as a general win. On
eICU it holds the best AUPRC (0.123) but the \emph{lowest} AUROC of the four
supervised models (0.658 vs.\ 0.677--0.702): it ranks the highest-risk hours
better---which is what an alerting system operating at a fixed alarm budget is
actually scored on---while ranking the full hour population slightly worse. A
deployment decision that weights global ranking, or that expects to re-threshold
freely, should prefer the monolith or the joint ensemble; one that fixes an alarm
budget should prefer the decomposition. Closing the AUROC gap---plausibly by
calibrating the organ heads and \emph{learning} their combination instead of
taking a hard maximum---is the clearest open item this work leaves.

\textbf{Is federation worth its cost?} On real hospitals FedAvg costs $-0.089$
AUPRC, which is over half the centralized model's headroom above no-skill
($0.089$ of $0.161$). That is a substantial price, not a footnote: federation is
worth paying for where pooling records is legally impossible, and our results say
to centralize wherever a data-use agreement permits it. The contribution is the
\emph{quantified} cost---feasibility alone would have been the misleading claim.

\section{Limitations}
External validation on eICU and federation across real hospitals address the
single-source and simulated-site limitations of an earlier draft, but limitations
remain. eICU label fidelity is imperfect: culture coverage is sparse, so suspicion of
infection is antibiotic-primary, and apparent Sepsis-3 prevalence is lower than in
MIMIC (3.6\% vs.\ 4.9\% positive hours); we therefore lead external comparisons with
AUROC and report AUPRC against the matched no-skill line. The missing-signal
robustness advantage, large internally, attenuates under domain shift. Labels are
retrospective and the escalation action is a proxy for real clinical response; the
internal test set has a few hundred positive episodes, so we lean on external splits
and CIs. The federated study uses a single shared model; federating the organ heads is
future work. Both databases are retrospective: the decisive test---a prospective,
silent-mode deployment measuring whether attributable alerts actually change
clinician behaviour and patient outcomes---remains future work, and nothing
reported here substitutes for it.

\section{Reproducibility and Ethics}
The pipeline is config-driven with fixed seeds; cohort, labels, features, training,
evaluation, and figures regenerate end-to-end from one command sequence. We state
exactly what is accessible. The repository contains the complete source package
(cohort construction and the Sepsis-3 derivation, the organ feature builder, the
per-organ risk heads and every baseline, the PPO alarm policy, the FedAvg
trainer, the eICU harmonization, the organ-blinding harness, and the figure
scripts); the YAML configuration files that fix every choice reported here
(inclusion criteria and site partition, antibiotic list, item-ID map, feature
manifest, label windows, and the MARL/PPO hyperparameters of
Sec.~\ref{subsec:hyper}); the CLI entry points that regenerate each table and
figure; and the test suite covering the Sepsis-3 derivation, the reward, leakage,
the MARL gates, the eICU harmonization, and the frozen-model invariant (no
gradient updates on external data). What is \emph{not} shared is data: MIMIC-IV
and eICU are credentialed under PhysioNet data-use agreements, so no raw or
derived row-level records---and no trained weights, which are fit on them---are
distributed; a credentialed user regenerates both by running the pipeline. Code
and analysis:
\url{https://github.com/MDSAZZADSIDDIQUE/sentinel}.

\section{Conclusion}
Organ-system decomposition gives an ICU early-warning model that matches or beats a
monolithic deep network, is uniquely robust to missing signals (a controlled result),
is interpretable, and is federatable at a measured cost---while cooperative
coordination, tested rigorously, provides no benefit. We argue for structure over
coordination in exogenous-dynamics clinical alerting.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
